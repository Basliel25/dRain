#include <stdio.h>
#include <assert.h>
#include "../include/drain.h"

int main(void) {
    Drain *d = drain_create(0.5);
    assert(d != NULL);

    uint64_t id;
    char   **params;
    int      len;

    return 0;
}
